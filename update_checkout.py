from pathlib import Path
p = Path('/home/ubuntu/ana-trufas-delivery/client/src/pages/Home.tsx')
s = p.read_text()
s = s.replace('  const [notes, setNotes] = useState("");', '''  const [notes, setNotes] = useState("");
  const [customer, setCustomer] = useState({ name: "", phone: "", address: "", number: "", neighborhood: "", complement: "" });
  const [payment, setPayment] = useState<"pix" | "cartao" | "dinheiro">("pix");''')
s = s.replace('''    if (!itemCount) return;
    const lines = cartItems.map((item) => `• ${item.quantity}x ${item.name} — ${money(item.price * item.quantity)}`);''', '''    if (!itemCount) return;
    if (!customer.name || !customer.phone || (delivery === "entrega" && (!customer.address || !customer.number || !customer.neighborhood))) {
      alert("Preencha seus dados e o endereço de entrega para continuar.");
      return;
    }
    const lines = cartItems.map((item) => `• ${item.quantity}x ${item.name} — ${money(item.price * item.quantity)}`);
    const address = delivery === "entrega" ? `${customer.address}, ${customer.number} — ${customer.neighborhood}${customer.complement ? ` (${customer.complement})` : ""}` : "Retirada na loja";
    const paymentLabel = payment === "pix" ? "Pix" : payment === "cartao" ? "Cartão" : "Dinheiro";''')
s = s.replace('''    const message = ["Olá, Ana Trufas! Quero fazer um pedido:", "", ...lines, "", `Subtotal: ${money(subtotal)}`, `Forma: ${delivery === "entrega" ? "Entrega" : "Retirada"}`, notes ? `Observações: ${notes}` : "", "", "Podem me confirmar disponibilidade e prazo?"] .filter(Boolean).join("\\n");''', '''    const message = ["Olá, Ana Trufas! Quero fazer um pedido:", "", `Cliente: ${customer.name}`, `Telefone: ${customer.phone}`, `Recebimento: ${delivery === "entrega" ? "Entrega" : "Retirada"}`, `Endereço: ${address}`, `Pagamento: ${paymentLabel}`, "", ...lines, "", `Subtotal: ${money(subtotal)}`, notes ? `Observações: ${notes}` : "", "", "Podem me confirmar disponibilidade, taxa e prazo?"] .filter(Boolean).join("\\n");''')
old = '''{cartItems.length > 0 && <div className="mt-8 border-t border-[#ead6d6] pt-6"><p className="mb-3 text-xs font-bold uppercase tracking-[.16em] text-[#9a777c]">como prefere receber?</p><div className="grid grid-cols-2 gap-2">{([['entrega', 'Entrega'], ['retirada', 'Retirada']] as const).map(([value, label]) => <button key={value} onClick={() => setDelivery(value)} className={`rounded-xl border px-3 py-3 text-sm font-bold ${delivery === value ? "border-[#8f294c] bg-[#f7e6e9] text-[#8f294c]" : "border-[#eadfe0] bg-white text-[#927277]"}`}>{label}</button>)}</div><textarea value={notes} onChange={(e) => setNotes(e.target.value)} placeholder="Alguma observação? (opcional)" className="mt-3 min-h-20 w-full resize-none rounded-xl border border-[#eadfe0] bg-white p-3 text-sm outline-none placeholder:text-[#b89b9f] focus:border-[#c77b93]" /></div>}'''
new = '''{cartItems.length > 0 && <div className="mt-8 border-t border-[#ead6d6] pt-6"><p className="mb-3 text-xs font-bold uppercase tracking-[.16em] text-[#9a777c]">1 · seus dados</p><div className="grid gap-2"><input value={customer.name} onChange={(e) => setCustomer({ ...customer, name: e.target.value })} placeholder="Nome completo *" className="h-11 rounded-xl border border-[#eadfe0] bg-white px-3 text-sm outline-none focus:border-[#c77b93]" /><input value={customer.phone} onChange={(e) => setCustomer({ ...customer, phone: e.target.value })} placeholder="Seu WhatsApp *" className="h-11 rounded-xl border border-[#eadfe0] bg-white px-3 text-sm outline-none focus:border-[#c77b93]" /></div><p className="mb-3 mt-6 text-xs font-bold uppercase tracking-[.16em] text-[#9a777c]">2 · como prefere receber?</p><div className="grid grid-cols-2 gap-2">{([['entrega', 'Entrega'], ['retirada', 'Retirada']] as const).map(([value, label]) => <button key={value} onClick={() => setDelivery(value)} className={`rounded-xl border px-3 py-3 text-sm font-bold ${delivery === value ? "border-[#8f294c] bg-[#f7e6e9] text-[#8f294c]" : "border-[#eadfe0] bg-white text-[#927277]"}`}>{label}</button>)}</div>{delivery === "entrega" && <div className="mt-3 grid gap-2"><input value={customer.address} onChange={(e) => setCustomer({ ...customer, address: e.target.value })} placeholder="Rua / avenida *" className="h-11 rounded-xl border border-[#eadfe0] bg-white px-3 text-sm outline-none focus:border-[#c77b93]" /><div className="grid grid-cols-[.55fr_1fr] gap-2"><input value={customer.number} onChange={(e) => setCustomer({ ...customer, number: e.target.value })} placeholder="Número *" className="h-11 rounded-xl border border-[#eadfe0] bg-white px-3 text-sm outline-none focus:border-[#c77b93]" /><input value={customer.neighborhood} onChange={(e) => setCustomer({ ...customer, neighborhood: e.target.value })} placeholder="Bairro *" className="h-11 rounded-xl border border-[#eadfe0] bg-white px-3 text-sm outline-none focus:border-[#c77b93]" /></div><input value={customer.complement} onChange={(e) => setCustomer({ ...customer, complement: e.target.value })} placeholder="Complemento (opcional)" className="h-11 rounded-xl border border-[#eadfe0] bg-white px-3 text-sm outline-none focus:border-[#c77b93]" /></div>}<p className="mb-3 mt-6 text-xs font-bold uppercase tracking-[.16em] text-[#9a777c]">3 · forma de pagamento</p><div className="grid grid-cols-3 gap-2">{([['pix', 'Pix'], ['cartao', 'Cartão'], ['dinheiro', 'Dinheiro']] as const).map(([value, label]) => <button key={value} onClick={() => setPayment(value)} className={`rounded-xl border px-2 py-3 text-xs font-bold ${payment === value ? "border-[#8f294c] bg-[#f7e6e9] text-[#8f294c]" : "border-[#eadfe0] bg-white text-[#927277]"}`}>{label}</button>)}</div><textarea value={notes} onChange={(e) => setNotes(e.target.value)} placeholder="Alguma observação? (opcional)" className="mt-3 min-h-20 w-full resize-none rounded-xl border border-[#eadfe0] bg-white p-3 text-sm outline-none placeholder:text-[#b89b9f] focus:border-[#c77b93]" /></div>}'''
if old not in s:
    raise SystemExit('checkout block not found')
s = s.replace(old, new)
p.write_text(s)
print('updated')
